import * as React from 'react';
import Box from '@mui/material/Box';

const ErrorMessage = () => {
  return (
    <Box sx={{ width: '100%' }}>
      There was an error
    </Box>
  );
}

export default ErrorMessage