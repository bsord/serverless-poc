import {NotesView} from '../components/NotesView';
import {Layout} from '../components/Layout';

export const Notes = () => {
  return (
    <Layout title="Notes">
        <NotesView/>
    </Layout>
  );
}